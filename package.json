{
  "name": "commonsku-reports-automation",
  "version": "3.0.0",
  "description": "Automated CommonSKU report exporter with modern UI",
  "main": "electron/dist/electron/src/main.js",
  "scripts": {
    "build": "tsc",
    "build:electron": "tsc -p electron/tsconfig.json",
    "build:ui": "cd ui && npm run build",
    "build:all": "npm run build && npm run build:electron && npm run build:ui",
    "start": "npm run build && node dist/index.js",
    "start:electron": "npm run build:all && electron .",
    "dev": "ts-node src/index.ts",
    "install-browsers": "playwright install chromium",
    "dashboard": "npm run build && node dist/index.js dashboard",
    "pipeline": "npm run build && node dist/index.js pipeline",
    "sales-orders": "npm run build && node dist/index.js sales-orders",
    "daily": "npm run build && node dist/index.js daily",
    "weekly": "npm run build && node dist/index.js weekly",
    "monthly": "npm run build && node dist/index.js monthly",
    "friday": "npm run build && node dist/index.js friday",
    "all": "npm run build && node dist/index.js all",
    "sr-daily": "npm run build && node dist/index.js sr-daily",
    "sr-weekly": "npm run build && node dist/index.js sr-weekly",
    "sr-friday": "npm run build && node dist/index.js sr-friday",
    "scheduled": "npm run build && node dist/index.js scheduled",
    "cron": "npm run build && node dist/index.js cron",
    "package": "npm run build:all && electron-builder"
  },
  "build": {
    "appId": "com.commonsku.automation",
    "productName": "CommonSKU Automation",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "electron/dist/**/*",
      "ui/build/**/*",
      "node_modules/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "ui/public/icon.ico"
    }
  },
  "keywords": [
    "commonsku",
    "automation",
    "playwright",
    "reports",
    "electron"
  ],
  "author": "Lawrence",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.30.0",
    "dotenv": "^16.3.1",
    "electron-store": "^8.1.0",
    "node-cron": "^3.0.3",
    "playwright": "^1.40.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/node-cron": "^3.0.11",
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
